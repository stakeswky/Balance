{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for Balance desktop windows",
  "windows": ["main", "startup-error", "tray"],
  "permissions": ["core:default", "updater:default"]
}
