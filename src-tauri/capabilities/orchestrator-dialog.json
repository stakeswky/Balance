{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "orchestrator-dialog",
  "description": "Allow the main orchestration window to choose a local repository folder",
  "windows": ["main"],
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:4780/*"]
  },
  "permissions": ["dialog:allow-open"]
}
